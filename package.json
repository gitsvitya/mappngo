{
  "name": "mappngo-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:html": "node scripts/generate-html.mjs",
    "predev": "npm run sync:html",
    "dev": "vite",
    "prebuild": "npm run sync:html",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.14"
  }
}
